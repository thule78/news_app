# Separate source retrieval from AI synthesis

Research first retrieves and stores source metadata and relevant evidence; only then may AI synthesize Claims from that stored material. Script generation receives supported Claims rather than unrestricted web access, preventing invented citations and making research reusable across Trips and model providers.
