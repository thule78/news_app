# Only unsanitized supplier data hard-blocks export

The MVP makes Quality Scores and validation failures visible but overridable by the Approver because the local CLI does not publish automatically. Detected Supplier Data still present in AI-bound or exported content is the only non-overridable Export Block, preserving the forbidden-data boundary while keeping editorial judgment with the human.
